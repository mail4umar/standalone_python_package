# standalone_python_package
I am trying to make an application that has a GUI and it can be installable. The backend would be entirely python.


# to create the package
pyinstaller --clean framework_v2.spec